using FiveNightsAtGorillas.Managers.NetworkedData;
using Photon.Pun;
using UnityEngine;

namespace FiveNightsAtGorillas.Managers.AI
{
    public class AIManager : MonoBehaviourPunCallbacks
    {
        /*public bool LoadedAIProps { get; private set; } = false;

        public override void OnJoinedRoom()
        {
            if (!LoadedAIProps)
            {
                PhotonData.instance.RoomProperties.Add("GorillaCam", "Cam11");
                PhotonData.instance.RoomProperties.Add("MingusCam", "Cam11");
                PhotonData.instance.RoomProperties.Add("BobCam", "Cam11");
                PhotonData.instance.RoomProperties.Add("DingusStage", "Stage1");
                PhotonNetwork.CurrentRoom.SetCustomProperties(PhotonData.instance.RoomProperties);
                LoadedAIProps = true;
            }
        }*/
    }
}